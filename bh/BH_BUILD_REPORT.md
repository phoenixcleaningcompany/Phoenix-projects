# BH (Beauty, Hair & Spa) build — completion report

**Status: COMPLETE — 505/505 pages, all hard gates green.**

| Metric | Result |
|---|---|
| `verify_bh.py outputs` | **505/505 pass all 16 hard checks** |
| Worst-pair dup (`score_bh.py`, binding guard) | **51.5%** (cap 52.0%), **0 pairs over cap** |
| Overall-unique (diagnostic only) | **68.4%** (documented ~66–68% structural ceiling; not chased) |
| Towns covered | all 505 on `BH_towns.csv` (rank 1–505), `next_towns.py` REMAINING = 0 |
| Unique `<title>` / meta description | 505 / 505 |

This matches/edges the RT precedent (RT shipped 505/505, worst pair 51.9%, overall 67.6%).

## How it was built
- **Flagship** `bh-london.html` is the base template AND the London page — never rebuilt, copied into `outputs/` last.
- 504 town pages generated by `python3 bh_build.py` from md5-picked prose pools + the per-town `TOWNS` dict in `bh_build.py`.
- Per-town data (region, 3 geographic nearby on CSV, snapshot, s1_head, **2 local paras**, kit_loc, s2_intro) was **web-researched** by background agents (10 batches), written to `research_bNNg*.py`, validated + spliced by `splice_batch.py`.

## Kit (this directory)
- `bh_build.py` · `bh-london.html` · `BH_towns.csv` · `SPEC.md` · `CLAUDE.md` · `BH_HANDOVER.md`
- Gates: `verify_bh.py` (binding) · `score_bh.py` · `diag_bh.py`
- Helpers authored this build: `next_towns.py` · `splice_batch.py` · `enrich.py` · `PROMPT_TEMPLATE.md`
- `research_b01g*.py` … `research_b10g*.py` — the web-researched town data (source of truth)
- `outputs/` — the 505 built pages

## Reproduce
```
cd bh
python3 bh_build.py            # builds all TOWNS into outputs/ (BH_OUTDIR overrides)
cp bh-london.html outputs/
python3 verify_bh.py outputs   # must read 505/505
python3 score_bh.py outputs    # worst pair <= 52%
```

## Kit patches applied (per handover §0/§0b)
- `diag_bh.py`: `import scobh_re` → `import score_bh` (was an ImportError).
- `bh_build.py` `main()`: `/mnt/user-data/outputs` → `BH_OUTDIR` / local `outputs/` fallback; display name resolved from exact CSV spelling (fixes hyphenated/multiword towns).
- `verify_bh.py`: corrected stale `verify_fs` / `se-*.html` header + error text.
- `pick()` confirmed already on md5 (no crc32 regression).

## Uniqueness handling (reactive enrichment)
The pooled scaffold puts a cluster of unlucky pairs near ~51.5% (a plateau, under cap). Only pairs that **exceed 52%** were fixed, via `enrich.py` adding a web-verified, proper-noun-rich sentence to the weaker page's `s1loc` (SPEC 8.3 — real local detail, never generic reword):
- **Denton / Rushden** 52.6% → cleared.
- **Beverley / Oadby** 52.9% → cleared.
- **Stevenage / Sutton-in-Ashfield** — proactively enriched for headroom.

One verify catch: **Stamford** prose contained the bleed term "garden centre" (RT retail signature); rewritten with bleed-free local detail (Red Lion Square, Scotgate).
</content>
